# だいどこ — 我が家のごはんサポート

公開 URL: **https://yukikanedomi.github.io/daidoko/**（GitHub Pages・リポジトリ `YukiKanedomi/daidoko`・2026-09-13 公開）。push すると Actions が `site/`＋`data/*.json` を配信する。

夫婦2人で使う、晩ごはんの「ゆるいお手伝いさん」。2026-09-12 開設（Yorozuya での相談から独立）。

## 芯（これを崩さない）

- **強制しない**。通知や催促はしない。開いたときだけ「今夜のこんだて」が1案大きく出ていて、「これにする」か「ほかの案」を見るだけ。作らなかった日は何も起きない。
- **考えさせない**（2026-09-13 オーナー）。価値は「考えるコストを減らす。パッと見て選ぶだけ」。手作りだけでなく、冷凍食品・素・レトルトなど「買えば一品になる物」も同列の選択肢として出す（台帳 `data/easy-products.json`）。説明文より選択肢。
- **評価に縛られない**（2026-09-13 オーナー）。台帳の評価（あり／なし・妻の一言）は参考で、献立を縛る制約は「避ける食材」だけ。最初の少ない評価で可能性を狭めない。提案は幅を保ち、たまに評価外のものも混ぜる。**評価はアプリの画面に書かない**（台帳の裏方に留める）。
- **入れ替えは軽く**（2026-09-13 オーナー「なんか違うなーという時に変更・入れ替えできると。複雑になりすぎるのも微妙」）: 「ほかの案 › もっと見る」で週の中の全料理から選べる。「この日は作らない」も1タップ。それ以上の編集機能は当面足さず、運用してみて次第で見直す（方針はガチガチに固めない。2026-09-13 オーナー）。
- **入力を義務にしない**。好みは「これにする」のタップと「ひとこと欄」から学ぶ。冷蔵庫の在庫管理のような重い入力は入れない。
- **相談は別の入り口**。「今夜どうしよう」「鶏むねが余ってる」は、当面 Claude Code とのチャット（この PJ のセッション）で受け、結果を次の提案に反映する（A案。画面内チャットは将来の拡張）。
- 妻と同じ画面。ログイン不要の URL をホーム画面に置く。

## 作り方（A案・定時生成型。2026-09-12 オーナー決定）

```
台帳(data/*.json) → 手元PCの定時ジョブ(claude -p)が献立候補と買い物リストを生成 → data/plan.json → 静的サイト(GitHub Pages) → 夫婦のスマホ
                     ↑ 「これにする」「ひとこと」はページの「コピー」→チャットに貼る round-trip、または後日 GitHub API で直接書き戻し
```

- 表示はビルド不要の静的構成（`site/` に index.html + app.js + styles.css）。壊れる部品を増やさない。
- 献立の生成は週2〜3回、`headless-job` スキルの作法で（PowerShell ランチャー・ps1 は ASCII・プロンプトは別 txt）。
- 料理写真は `imagegen` スキル（Codex）で統一トーン（俯瞰・白い皿・淡い木のテーブル・自然光・1:1）に生成し `site/photos/` に置く。写真の無い献立を画面に出さない。

## デザイン（2026-09-12 決定・`design/d-*.html` が基準。home/shop/week/book/recipe に加え weekend（土日に開く『今週末』＝仕込み＋店ごとの買い出し＋来週の平日）の6画面）

- 世の中の一級実例（Mela・Crouton・Mealime・AnyList・クラシル・me:new 等、`research/` に17本の実画面）の文法で組む。**メタファー演出はしない**（冷蔵庫・品書き・チラシの3案はオーナー判断で不採用）。
- 白地・黒文字・アクセント1色（`#e8532e`）・Inter + Noto Sans JP・角丸20/16/12px・絵文字禁止（アイコンは線画SVG）。
- **キャラクター**（2026-09-13 オーナー「アイコンのテイストで少し華やかに」）: アプリアイコンのマンドゥ2人組を `site/chara/*.png`（透過・6ポーズ: peek/apron/basket/steamer/sleep/cheer）として、**意味のある場所だけ**に置く。各画面ヘッダー右（今夜=peek、今週末=apron、買い物=basket、こんだて表=steamer）、案がない日=sleep、「これにする」「やる」のトースト=cheer。脇役はブロッコリーと卵（`broccoli`/`egg`/4人組の `friends`。2026-09-13 オーナー選定「バランスがいい」）: せいろの日はヘッダーが4人組、卵の日は卵、健康の一言の横にブロッコリー。ほかの候補7体は `design/chara/cast-grid.png`（豆腐・餃子・土鍋など、必要になったら透過化）。乱用しない。追加は `design/chara/ref.png`（アイコン）を参照させて imagegen で同じ顔に揃える。
- 構成: 今夜（曜日ストリップ→主案の大きな写真カード→ほかの案2枚→買い出し要約→ひとこと欄）／こんだて表／買い物（店ごと・売り場カテゴリ別チェックリスト、「家にありそうな物」は薄く）。**台帳タブは 2026-09-13 に廃止**（オーナー「アプリ内に台帳はいらないかも」。台帳は生成の裏方として `data/` に残す）。
- **幅を広げる枠「今週の新顔」**（2026-09-13 オーナー）: 週末画面と買い物に、実在の具体的な商品を1つだけ提示（`data/easy-products.json` の `try_next`）。商品名は捏造しない。棚にあるかは未確認のものは未確認と書く。**商品はメーカー公式ページへリンク**（写真はそこで見る。ブランド商品の画像は生成しない）、**作るものは信頼できるレシピサイトへリンク**（白ごはん.com／クラシル／DELISH KITCHEN／Nadia）。URL は生存確認してから入れる（2026-09-13 オーナー要望）。
- **健康は軽いルール**（2026-09-13 オーナー「ある程度気をつけたい」）: 毎食野菜1つ・魚は週2・揚げ物は週1まで・汁物を毎日インスタントにしない・主菜は肉魚豆腐卵を回す（`data/family.json` の `health`）。数値管理はしない。**豆知識**（2026-09-13 オーナー「強制はせず、栄養価があるものは説明してくれると選ぼうかなと思う」）: 主菜と仕込みのカードに栄養の一言を1行添える（`plan.json` の `tip`）。事実に忠実、効能は「〜に関わる」「〜とされる」の簡易な言い回しで添える（2026-09-13 オーナー「断定しなくていいが簡易には書いて」）、押しつけない。
- 主菜だけでなく副菜・汁物までセットで出す。所要時間（15/30/50分）を先頭に。
- **アプリアイコン**（2026-09-13 決定）: ゆるキャラ系ミニマル「せいろの中に顔つきマンドゥ2つとブロッコリー」。マスターは `design/icon/icon-master.png`（1024px・クリーム地 #fbf6ec）。検討経緯は `design/icon/grid1-8.png`（36案）。サイト組み込み時は `app-icon` スキルで一式生成。
- **平日は買い足しできない前提**（帰宅が21時近く。寄れてもコンビニ）。平日案は冷凍ストック・常備品・コンビニで組み、買い物リストは土日のまとめ買い用として出す（2026-09-12 オーナー）。

## データ（台帳・すべて JSON、`data/`）

- `family.json` — 家族の好き・苦手・避ける食材、人数、平日/週末の時間感覚
- `dishes.json` — 定番と履歴（名前・主材料・所要時間・タグ・最後に作った日・作った回数）
- `stores.json` — 近所の店（名前・駅からの距離・営業時間・得意分野・安い曜日）。土日はアリオ橋本のイトーヨーカドー＋カルディ＋久世福商店、ミウィ橋本のカルディ、近くの肉屋（寿々木）・魚屋（魚勝）、家の前のドンキ、帰り道のローソン・ファミマ、たまにロピア・イオン・まいばすけっと（2026-09-13 時点 11 店）
- `prep-ideas.json` — 週末仕込みの候補と評価（学習の記録）
- `veg-easy.json` — その日に5分で1品の野菜副菜（レシピ役割つき）
- `easy-products.json` — 買えば一品になる市販品（冷凍・素・レトルト・ミールキット）
- `plan.json` — 生成結果（日付ごとの主案＋ほかの案、買い物リスト、ひとこと）

## 機微情報

- 家族の好み・献立・店名は公開リポジトリに置いてよい（オーナー方針: 家計・家族の予定は自由。識別情報＝自宅住所・電話・会員番号は置かない）。店は店名と最寄り情報のみで、自宅住所そのものは `data/` に書かない。

## 進め方の記録

- `BACKLOG.md` — やることの候補と優先順（2026-09-13 起票。せいろガイド／食材の使い回し／バズレシピ収集／チラシ取り込み／自動生成 など）

- `design/` — 画面案（a/b/c は不採用の3案、d-* が採用方向）と `shot.ps1`（390×844 の描画確認）
- `research/` — App Store 公開データから集めた人気アプリの実画面（`apps.json`、`sheet-*.png`）
- 有力な方向（2026-09-12 オーナー）: **週末の仕込みと買い出しが本体**で、平日はその消化。「今夜1案」は残しつつ、重心を土日（仕込み候補＋店ごとの買い出し）に寄せる案を作戦として検討中。
- 2026-09-13: オーナー「組んでみるのとアプリも作り始めよう。あとで大きく作り変えるかも、試行錯誤で」→ `site/` に静的アプリを実装（index.html / app.js / styles.css、ハッシュルーティング、状態は localStorage、`?today=YYYY-MM-DD` で日付を偽装して確認）。`data/plan.json` は初回手組み。写真は `site/photos/*.jpg`（800px）。描画確認は Python の Playwright（scratchpad の shoot.py 方式、headless Chrome 直叩きは fetch 完了前に撮れて不安定）
- 定時ジョブ（献立生成）は未着手。当面は plan.json をチャットで更新する
